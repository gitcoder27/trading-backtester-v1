{
  "name": "trading-backtester-charts",
  "version": "1.0.0",
  "description": "Chart library management for trading backtester",
  "private": true,
  "scripts": {
    "install-charts": "npm install",
    "build-charts": "npm run build",
    "build": "node scripts/build-charts.js",
    "update-charts": "node scripts/update-charts.js",
    "check-updates": "node scripts/update-charts.js --check-only",
    "build": "node scripts/build-charts.js",
    "build-version": "node scripts/build-charts.js",
    "clean": "rimraf webapp/static/js/lightweight-charts.*.js"
  },
  "dependencies": {
    "lightweight-charts": "^5.0.8"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}