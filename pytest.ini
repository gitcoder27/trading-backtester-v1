[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow to run
    integration: marks integration tests
addopts = --color=yes -ra -q --cov=backtester --cov-report=html --cov-report=term
